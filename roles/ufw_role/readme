---
- name: Configurer le pare-feu ufw sur serveurs Debian/Ubuntu
  hosts: all
  become: yes

  roles:
    - role: ufw_role
      vars:
        ufw_enabled: true
        ufw_allow_ports:
          - "22"
          - "80/tcp"
          - "443/tcp"
        ufw_deny_ports: []
        ufw_allow_services:
          - "ssh"
        ufw_default_input_policy: "deny"
        ufw_default_output_policy: "allow"
# Ce rôle installe et configure le pare-feu ufw pour sécuriser les accès réseau,
# en gérant les règles d’autorisation et de blocage sur ports et services,
# ainsi que l’état global d’activation du firewall.
