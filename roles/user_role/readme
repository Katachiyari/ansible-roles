---
- name: Gérer un utilisateur système avec le rôle user_role
  hosts: all
  become: yes

  roles:
    - role: user_role
      vars:
        user_name: "deploy_user"
        user_uid: 1500
        user_group: "deploy"
        user_groups:
          - "sudo"
          - "docker"
        user_shell: "/bin/bash"
        user_password: "$6$rounds=4096$EXAMPLE$hashedpasswordhere"  # Mot de passe chiffré
        user_create_home: true
        user_state: present
# Ce rôle crée ou modifie un utilisateur système avec groupes, shell, home et mot de passe,
# ou le supprime si demandé, facilitant la gestion des comptes utilisateurs sur vos serveurs.
