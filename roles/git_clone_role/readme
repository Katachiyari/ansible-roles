---
- name: Cloner un dépôt Git dans le répertoire cible
  hosts: all
  become: yes

  roles:
    - role: git_clone_role
      vars:
        git_repo_url: "https://github.com/example/project.git"
        git_version: "main"
        git_dest: "/opt/project"
        git_owner: "www-data"
        git_group: "www-data"
        git_force: true
