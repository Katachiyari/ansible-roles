---
- name: Déployer un fichier de configuration via template
  hosts: all
  become: yes

  roles:
    - role: template_role
      vars:
        template_src: "myconfig.conf.j2"    # Chemin relatif dans le dossier templates du rôle
        template_dest: "/etc/myapp/myconfig.conf"
        template_owner: "root"
        template_group: "root"
        template_mode: "0644"
# Ce rôle déploie un fichier via un template Jinja2, permettant d’adapter
# dynamiquement la configuration selon les variables fournies.
